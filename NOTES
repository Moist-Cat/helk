> function second(a, b, c) => b

Compute closure.

> base

base(self) // <- we have the type inferred
degrades the type
that is, gets self.parent and calls the fun


>block is a scope

Efficient during codegen (it's simply a hint)

Be careful about strings named as variables!

let a  = Point("a") // error!
