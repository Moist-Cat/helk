type Point {
    x = 0;
    y = 0;
};

type Point2 inherits Point {
    z = 1;
};

let a = new Point2(3, 4, 5) in print(a.x);
let b = new Point2(3, 4, 5) in print(b.y);
let c = new Point2(3, 4, 5) in print(c.z);
